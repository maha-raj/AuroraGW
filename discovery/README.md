# Discovery relay (mDNS + SSDP)

AuroraGW uses **alsmith/multicast-relay** to relay mDNS + SSDP between segments
(for Chromecast/FireTV/Samsung TV discovery across subnets).

This kit downloads the upstream script at install time.
